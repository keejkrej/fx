-- Workspace Lua plugins. Two shipped demos of the plugin API:
-- `lua/diffview` (in-TUI diff + comments-to-input) and
-- `lua/clipboard_paste` (Linux/Windows screenshot buffer paste via fx.paste.hook;
-- macOS clipboard images stay first-class in core).
require("diffview")
require("clipboard_paste")
